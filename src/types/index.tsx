export interface SelectType {
  value: string;
  text: string | number;
}

export interface FetchCarType {
  make: string;
  model: string;
  fuel_type: string;
  transmission: string;
  year: string;
}
